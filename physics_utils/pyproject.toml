[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "physics_utils"
version = "0.1.0"
description = "Utility functions for physics workflows"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["physics_utils*"]
